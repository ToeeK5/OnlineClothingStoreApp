<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Màu nền của card -->
    <solid android:color="#F8F6F5"/>

    <!-- Đường viền (stroke) -->
    <stroke
        android:width="1dp"
        android:color="#DDD6D2"/>

    <!-- Độ bo góc -->
    <corners android:radius="12dp"/>

</shape>